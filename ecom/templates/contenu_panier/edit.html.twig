{% extends 'base.html.twig' %}

{% block title %}{{ 'ContenuPanier.Titre.Edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'ContenuPanier.Nom.Edit'|trans }}</h1>

    {% if isAdmin == 1 %}
        {{ include('contenu_panier/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('contenu_panier/_delete_form.html.twig') }}
        <a  class="btn btn-primary" href="{{ path('contenu_panier_index') }}">{{ 'Global.Retour'|trans }}</a>
    {% else %}
        <p>{{ 'Global.Acces'|trans }}</p>
    {% endif %}

{% endblock %}
