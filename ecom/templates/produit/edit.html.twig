{% extends 'base.html.twig' %}

{% block title %}{{ 'Produit.Titre.Edit'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Produit.Nom.Edit'|trans }}</h1>

    {% if isAdmin == 1 %}
        {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('produit/_delete_form.html.twig') }}
        <a  class="btn btn-primary" href="{{ path('produit_index') }}">{{ 'Global.Retour'|trans }}</a>
    {% else %}
        <p>{{ 'Global.Acces'|trans }}</p>
    {% endif %}
{% endblock %}
